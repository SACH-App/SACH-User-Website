feat(ui): implement FIR details mapping and Phase 3 real-time alerts

This commit encapsulates extensive UI/UX improvements, real-time notification capabilities, and data mapping fixes for the Citizen Portal.

Frontend FIR Management UI:
- Fixed case-sensitivity bug in MyFirsPage.jsx filters and DashboardPage.jsx statistics, ensuring frontend status checks accurately match backend-compliant lowercase enums ("pending" instead of "Pending").
- Completely revamped FirDetailPage.jsx to display previously missing backend data, including the FIR title and priority levels.
- Fixed the evidence attachment section by correcting a typo mapping (fir.evidences -> fir.evidence), ensuring uploaded files are accessible.
- Repaired the Assigned Officer UI card to correctly render fir.officer_name rather than an undefined nested object.
- Built an elegant new "Official Updates" timeline block on the FIR details page to beautifully display system notifications and admin comments.
- Designed and injected a styled UI placeholder for future map integrations under the information grid.

Phase 3 Real-Time Notifications:
- Implemented Optimistic UI updates in AlertStore.jsx for both individual and batch "mark as read" functions, completely eliminating UI lag and making notification clearing feel instantaneous.
- Enabled automatic background polling (10-second intervals) in the Alerts store to seamlessly pull in new notifications without manual browser refreshes, satisfying the core Phase 3 requirement for "Real-Time Updates".
- Enhanced user profile verification flows and alert polling logic to only execute when a profile is actively authenticated.
